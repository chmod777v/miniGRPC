apiVersion: v1
kind: Service
metadata:
  name: prometheus-service
spec:
  selector:
    app: prometheus
  ports:
    - name:
      port: 9090
      targetPort: 9090
      protocol: TCP
  type: ClusterIP