global:
  scrape_interval: 3s
  evaluation_interval: 3s

scrape_configs:
  - job_name: "server"
    scrape_interval: 3s
    scrape_timeout: 3s
    metrics_path: /metrics
    static_configs:
      - targets: ["server:8081"]